"""Software-engineering layer for the Smart Lost & Found service.

The `ai/` package (provided, untouched) does the vision/embedding work.
Everything under `src/` is the team's own code: config, storage, API,
CLI, concurrency, retries, validation, logging, telemetry.
"""
